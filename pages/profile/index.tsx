// Detailed profile page.

// Show name.
// Show nr ascociated projects and role on those.
// Show nr of projects on calc assign to profile.

import React from 'react';

import PageLoadingSpinner from '@components/functional/pageLoadingSpinner/PageLoadingSpinner';

const Profile = () => (
    <div className="Page">
        <PageLoadingSpinner size={50} />
        <h5>Loading page...</h5>
    </div>
);

export default Profile;
