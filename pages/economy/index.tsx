import React from 'react';
import PageLoadingSpinner from '@components/functional/pageLoadingSpinner/PageLoadingSpinner';
// Detailed economics page.

// Show overall economy.
// Show slider for economy overview of each active project.

// Show % to finish in time and % of costs of total.

const Economy: React.FC = () => (
    <div className="Page">
        <PageLoadingSpinner size={50} />
        <h5>Loading page...</h5>
    </div>
);

export default Economy;
